# Только PostgreSQL для локальной разработки (Docker Desktop).
# Схема приложения (не public) создаётся API/Admin при старте из DATABASE_SCHEMA.
# Ручное воссоздание: server/db/recreate_schema.sql

services:
  postgres:
    image: postgres:16-alpine
    container_name: react-chat-bot-db
    restart: unless-stopped
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-chatbot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-chatbot}
      POSTGRES_DB: ${POSTGRES_DB:-chatbot}
    volumes:
      - chatbot_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-chatbot} -d ${POSTGRES_DB:-chatbot}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  chatbot_pg_data:
